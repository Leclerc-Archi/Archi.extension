<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Archi- | BIM-TEAM Audit Multi-Gabarits" Height="750" Width="1100" 
        WindowStartupLocation="CenterScreen" Background="#F8F4ED">
    
    <Window.Resources>
        <SolidColorBrush x:Key="ColorPrimary" Color="#166534"/>
        <SolidColorBrush x:Key="ColorSecondary" Color="#D1CFC0"/>
        <SolidColorBrush x:Key="ColorAccentGreen" Color="#15803D"/>
        <SolidColorBrush x:Key="ColorAccentOrange" Color="#D97706"/>
        <SolidColorBrush x:Key="ColorAccentSand" Color="#F2EBDC"/>
        
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        </Style>
        
        <Style x:Key="NaturalListBoxStyle" TargetType="ListBox">
            <Setter Property="BorderBrush" Value="{StaticResource ColorSecondary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{StaticResource ColorAccentSand}"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ListBoxItemStyle}"/>
            <Setter Property="SelectionMode" Value="Extended"/>
        </Style>
        
        <DataTemplate x:Key="CheckableItemTemplate">
            <CheckBox IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" 
                      Margin="4,4" IsHitTestVisible="False">
                <TextBlock Text="{Binding}" VerticalAlignment="Center" Foreground="#333333"/>
            </CheckBox>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="*"/>    
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="AUDIT ET COMPARATEUR MULTI-GABARITS" FontSize="18" FontWeight="Black" Foreground="{StaticResource ColorPrimary}"/>
            <Rectangle Height="3" Fill="{StaticResource ColorSecondary}" HorizontalAlignment="Stretch" Margin="0,8,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.5*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                <TextBlock Text="Gabarit de RÉFÉRENCE :" VerticalAlignment="Center" FontWeight="Bold" Foreground="#444444" Margin="0,0,10,0"/>
                <ComboBox x:Name="ComboReference" Width="300" Height="32" VerticalContentAlignment="Center" Padding="8,0"/>
            </StackPanel>
            
            <Grid Grid.Column="1">
                <TextBox x:Name="SearchBox" Height="32" VerticalContentAlignment="Center" Padding="8,0,8,0" FontSize="13"
                         BorderBrush="{StaticResource ColorSecondary}" Background="{StaticResource ColorAccentSand}"/>
                <TextBlock Text="Filtrer les gabarits..." Foreground="#A6A291" IsHitTestVisible="False" Margin="12,0,0,0" 
                           VerticalAlignment="Center" FontStyle="Italic" FontSize="13">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2.5*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,15,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Gabarits à comparer :" FontSize="13" FontWeight="Bold" Foreground="{StaticResource ColorPrimary}" Margin="0,0,0,8"/>
                <ListBox Grid.Row="1" x:Name="TemplateList" Style="{StaticResource NaturalListBoxStyle}" ItemTemplate="{StaticResource CheckableItemTemplate}"/>
            </Grid>

            <Grid Grid.Column="1" Margin="15,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <DockPanel Grid.Row="0" Margin="0,0,0,8" LastChildFill="False">
                    <TextBlock DockPanel.Dock="Left" Text="Matrice comparative des filtres" FontSize="13" FontWeight="Bold" Foreground="{StaticResource ColorPrimary}" VerticalAlignment="Center"/>
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                        <Button x:Name="BtnRefresh" Content="RÉACTUALISER" Width="120" Height="26" Margin="0,0,10,0" Background="{StaticResource ColorAccentGreen}"/>
                        <Button x:Name="BtnApply" Content="SYNCHRONISER" Width="120" Height="26" Background="{StaticResource ColorAccentOrange}"/>
                    </StackPanel>
                </DockPanel>

                <DataGrid Grid.Row="1" x:Name="AuditGrid" AutoGenerateColumns="False" IsReadOnly="False" CanUserAddRows="False" CanUserDeleteRows="False" SelectionUnit="Cell"
                          GridLinesVisibility="All" Background="White" RowHeaderWidth="0"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          BorderBrush="{StaticResource ColorSecondary}" HorizontalGridLinesBrush="#E5E5E5" VerticalGridLinesBrush="#E5E5E5">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#166534"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="8,6"/>
                            <Setter Property="FontSize" Value="12"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                </DataGrid>
            </Grid>
        </Grid>
    </Grid>
</Window>