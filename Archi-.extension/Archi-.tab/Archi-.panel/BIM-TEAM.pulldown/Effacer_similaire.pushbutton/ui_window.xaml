<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Archi- | BIM-TEAM Élimination des Doublons" Height="650" Width="900" 
        WindowStartupLocation="CenterScreen" Background="#F8F4ED">
    
    <Window.Resources>
        <SolidColorBrush x:Key="ColorPrimary" Color="#166534"/>
        <SolidColorBrush x:Key="ColorSecondary" Color="#D1CFC0"/>
        <SolidColorBrush x:Key="ColorAccentGreen" Color="#15803D"/>
        <SolidColorBrush x:Key="ColorAccentSand" Color="#F2EBDC"/>
        
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        </Style>
        
        <Style x:Key="NaturalListBoxStyle" TargetType="ListBox">
            <Setter Property="BorderBrush" Value="{StaticResource ColorSecondary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{StaticResource ColorAccentSand}"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ListBoxItemStyle}"/>
            <Setter Property="SelectionMode" Value="Extended"/>
        </Style>
        
        <DataTemplate x:Key="CheckableItemTemplate">
            <Grid Background="Transparent">
                <CheckBox IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" 
                          VerticalAlignment="Center" Margin="2,4" IsHitTestVisible="False">
                    <TextBlock Text="{Binding DisplayLabel}" VerticalAlignment="Center" Margin="5,0,0,0" Foreground="#333333"/>
                </CheckBox>
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="*"/>    
            <RowDefinition Height="Auto"/> 
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="ÉLÉMENTS DUPLIQUÉS DÉTECTÉS" FontSize="18" FontWeight="Black" Foreground="{StaticResource ColorPrimary}"/>
            <TextBlock Text="Sélectionnez les doublons à supprimer de la maquette. Utilisez Ctrl+A ou Shift+Clic pour aller plus vite." FontSize="12" Foreground="#666666" Margin="0,4,0,0"/>
            <Rectangle Height="3" Fill="{StaticResource ColorSecondary}" HorizontalAlignment="Stretch" Margin="0,8,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            
            <Grid Grid.Column="1">
                <TextBox x:Name="SearchBox" Height="32" VerticalContentAlignment="Center" Padding="8,0,8,0" FontSize="13"
                         BorderBrush="{StaticResource ColorSecondary}" Background="{StaticResource ColorAccentSand}"/>
                
                <TextBlock Text="Filtrer par catégorie, type, ID..." Foreground="#A6A291" IsHitTestVisible="False" Margin="12,0,0,0" 
                          VerticalAlignment="Center" FontStyle="Italic" FontSize="13">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="{x:Static sys:String.Empty}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Grid>

        <Grid Grid.Row="2">
            <ListBox x:Name="DuplicateList" Style="{StaticResource NaturalListBoxStyle}" ItemTemplate="{StaticResource CheckableItemTemplate}"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button x:Name="BtnSelectAll" Content="Tout cocher" Width="110" Height="34" Margin="0,0,10,0" Background="#7C7A6E"/>
                <Button x:Name="BtnUnselectAll" Content="Tout décocher" Width="110" Height="34" Background="#A6A291"/>
            </StackPanel>
            
            <Button x:Name="BtnDelete" Grid.Column="1" Content="SUPPRIMER LA SÉLECTION" Width="220" Height="36" 
                    HorizontalAlignment="Right" Background="#D9534F"/>
        </Grid>
    </Grid>
</Window>